-- Nenhuma alteracao de schema: ManualModel::search() foi ajustado apenas para
-- usar nomes de parametros nomeados distintos (correcao de bind duplicado no
-- PDO), sem adicionar/alterar colunas, indices ou tabelas.
SELECT 1;
